return {
    {
        "mfussenegger/nvim-jdtls",
        ft = { "java" },
        config = function()
            -- You can require and configure nvim-jdtls here, e.g.:
            -- require('jdtls').start_or_attach({ ... })
        end,
    },
}
