return {
    {
        "stephpy/vim-yaml",
        lazy = false,                 -- load on startup; set to true if you want lazy loading
        ft = { "yaml", "azure-pipelines" }, -- filetypes to load this plugin for
    },
}
