-- lua/modules.lua
return {
    -- Core modules
    ui = true, -- UI enhancements
    editor = true,
    ChristofferNissen = true, -- Personal configuration

    -- Language modules
    yaml = true, -- YAML support
    -- python = false,  -- Uncomment to add more modules
    -- typescript = false,
}
